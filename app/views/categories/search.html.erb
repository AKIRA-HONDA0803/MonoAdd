<h1 class="search-title">モノ一覧</h1>
<% if params[:keyword] %>
  <h4>「<%= params[:keyword] %>」の検索結果：<%= @search_category.count %>件</h4>
<% end %>

<% if params[:category_id] %>
  <h4>「<%= @category.name %>」に該当するモノ：<%= @search_category.count %>件</h4>
<% end %>

<% if params[:address_id] %>
  <h4>「<%= @address.name %>」にあるモノ：<%= @search_category.count %>件</h4>
<% end %>

<div class="search-items">
  <% @search_category.each do |item| %>
    <div class="search-item-list">
      <%= link_to edit_item_path(item), class: "link" do %>
        <div class="item-picture">
          <% if item.picture.attached? %>
            <%= image_tag item.picture,class: "picture" %>
          <% else %>
            <%= image_tag 'favicon.ico', class: "picture"  %>
        　<% end %>
      　</div>
        <%= link_to search_address_path(address_id: item.address.id), class: "link" do %>
          <div class="search-item-add">
            #<%= item.address.name %>
          </div>
        <% end %>
        <%= link_to search_category_path(category_id: item.category.id), class: "link" do %>
          <div class="search-item-category">
            #<%= item.category.name %>
          </div>
        <% end %>
      <% end %>
      <div class="search-item-name">
          <%= item.name %>
        </div>
    </div>
  <% end %>
</div>
